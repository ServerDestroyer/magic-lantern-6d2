diff --git a/hw/eos/mpu.c b/hw/eos/mpu.c
--- a/hw/eos/mpu.c
+++ b/hw/eos/mpu.c
@@ -1288,6 +1288,7 @@ void mpu_spells_init(void)
     MPU_BUTTON_CODES(7D)
     MPU_BUTTON_CODES(EOSM)
     MPU_BUTTON_CODES(200D)
+    MPU_BUTTON_CODES(6D2)
     MPU_BUTTON_CODES_OTHER_CAM(EOSM2, EOSM)
 
     if (!button_codes)
diff --git a/hw/eos/mpu_spells/button_codes.h b/hw/eos/mpu_spells/button_codes.h
index bd46b35763..bb8ee2497c 100644
--- a/hw/eos/mpu_spells/button_codes.h
+++ b/hw/eos/mpu_spells/button_codes.h
@@ -654,3 +654,40 @@ static int button_codes_200D[] = {
     [GMT_GUICMD_PRESS_BUTTON_SOMETHING] = 0x2601,
     [BGMT_END_OF_LIST]                  = 0x0000
 };
+static int button_codes_6D2[] = {
+    [BGMT_MENU]                         = 0x0001,
+    [BGMT_INFO]                         = 0x0101,
+    [BGMT_PLAY]                         = 0x0301,
+    [BGMT_TRASH]                        = 0x0401,
+    [BGMT_UNPRESS_ZOOM_IN]              = 0x0800,
+    [BGMT_PRESS_ZOOM_IN]                = 0x0801,
+    /* The 6D2 has no physical zoom-out button; ROM switch ids 0x09/0x0A exist
+     * and carry the same internal roles as on the 200D (same DIGIC 7 switch
+     * numbering), so the internal zoom-out codes are used here. Required:
+     * mpu.c's key_map check exit(1)s on any gui_code with no entry. */
+    [BGMT_PRESS_ZOOM_OUT]               = 0x0A01,   /* internal */
+    [BGMT_UNPRESS_ZOOM_OUT]             = 0x0A00,   /* internal */
+    [BGMT_UNPRESS_UDLR]                 = 0x0B00,
+    [BGMT_PRESS_UP]                     = 0x0B02,
+    [BGMT_PRESS_UP_RIGHT]               = 0x0B03,
+    [BGMT_PRESS_UP_LEFT]                = 0x0B04,
+    [BGMT_PRESS_RIGHT]                  = 0x0B05,
+    [BGMT_PRESS_LEFT]                   = 0x0B06,
+    [BGMT_PRESS_DOWN_RIGHT]             = 0x0B07,
+    [BGMT_PRESS_DOWN_LEFT]              = 0x0B08,
+    [BGMT_PRESS_DOWN]                   = 0x0B09,
+    [BGMT_UNPRESS_SET]                  = 0x0C00,
+    [BGMT_PRESS_SET]                    = 0x0C01,
+    [BGMT_WHEEL_RIGHT]                  = 0x0D01,
+    [BGMT_WHEEL_LEFT]                   = 0x0DFF,
+    [BGMT_WHEEL_DOWN]                   = 0x0E01,
+    [BGMT_WHEEL_UP]                     = 0x0EFF,
+    [GMT_GUICMD_LOCK_ON]                = 0x1101,
+    [GMT_GUICMD_CLOSE_SLOT_COVER]       = 0x1200,
+    [GMT_GUICMD_OPEN_SLOT_COVER]        = 0x1201,
+    [BGMT_Q]                            = 0x2001,
+    [BGMT_LV]                           = 0x2101,
+    [BGMT_LIGHT]                        = 0x2301,
+    [GMT_GUICMD_PRESS_BUTTON_SOMETHING] = 0x2601,
+    [BGMT_END_OF_LIST]                  = 0x0000
+};
